{
  "name": "switch-to-eu-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "dev:proxy": "sudo caddy run --config Caddyfile",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.8.3"
  },
  "packageManager": "pnpm@9.5.0",
  "engines": {
    "node": "24"
  }
}